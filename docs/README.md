# Docs Layout

Non-runtime documents that were previously stored in the repo root are grouped here.

- `project/`: project requirements and planning notes
- `reference/`: external or long-form reference material
- `prompts/`: prompt collections kept for reference, but not auto-loaded by the app

Current runtime prompt presets remain under `prompt/`.
